# T007: authoritative traceability and release-readiness evidence

Task: `T007`

Kind: `pm`
Status: `done`

## Summary

The repository now has an executable, durable traceability check and clean
package/reproducibility evidence. The current source snapshot used for the
traceability result was `a45d4c0a7ec6057ae038782c7628e510a31e523b` with tree
`fd0b781c9e00dd2196d45a8bbcc900f5c9b46224`. The generated traceability result
passed for 89 planning Markdown files and 1,073 unique requirement records:
52 substantive requirements and 1,021 index-only plan records. It reported no
unmapped plan files, duplicate IDs, invalid statuses/evidence fields, or
repository-path escapes.

The current narrative RC3 status sections are receipt-driven and preserve
historical RC2/old-candidate identities. They do not claim that hosted CI,
provider acceptance, immutable OCI publication, deployment, rollback, human
approval, IANA registration, independent interoperability, or stable
publication has occurred.

## Durable outputs

- `.generated/requirements/requirements.json`
- `.generated/requirements/traceability-check.json`
- `IMPLEMENTATION_LEDGER.md`
- `.generated/launch/package-smoke.json`
- `.generated/launch/reproducibility.json`
- `.generated/launch/sbom-manifest.json`
- `.generated/launch/verify.json`
- `.generated/launch/repository-identity.json`
- `.generated/evidence/` local evidence bundles

The traceability and ledger files are generated by the repository commands and
are not hand-edited.

## Verification

- `pnpm install --frozen-lockfile` passed with pnpm 10.6.0.
- `pnpm traceability:check` passed: 89 plan files, 1,073 records, 1,073
  unique IDs, source identity recorded, and clean source worktree under the
  generated-file policy.
- `pnpm format:check` passed.
- `pnpm lint` passed.
- `pnpm typecheck` passed for all six workspace packages and tools.
- `pnpm test` passed: 26 files and 75 tests.
- `pnpm workflow:check` passed for all seven GitHub/Forgejo workflows.
- `pnpm docs:bundle:check`, content, feeds, release, routes, security,
  license, SEO, and rollback policy checks passed.
- `pnpm build:verify` and `pnpm build:web` passed with 74 generated pages.
- `pnpm package:smoke` passed five clean temporary package-consumer installs,
  type imports, and runtime imports without changing the repository lockfile.
- `pnpm reproducibility:check` passed two controlled runs: 7,559 bytes with
  SHA-256 `ee712fd1b3a3ed053024f6511f262b35a3059e382cf0d4488705658690a054d6`.
- Browser checks passed: E2E 7/7, RC accessibility 21/21, link crawl 1/1,
  visual baselines 3/3, production browser 12/12, and 13-state visual
  capture. Lighthouse/performance passed 12 runs across six routes and the
  configured budgets.
- `pnpm sbom:generate` passed for 867 locked packages; secret scanning passed.
  The four development-tool advisory groups remain recorded by the
  vulnerability check and are not hidden.
- The aggregate `pnpm verify` passed through all portable, build, package,
  reproducibility, and browser tasks, then failed closed at
  `pnpm container:check` because the local Docker build timed out after the
  configured 15 seconds. No Docker pass is claimed.

## Hosted and external boundaries

GitHub/Forgejo execution, CodeQL, dependency review, cross-platform hosted
receipts, Docker-backed validation, staging/provider behavior, OCI
publication, signing/provenance, release-specific SLO, rollback, legal,
privacy, factual, media, governance, IANA, independent interoperability,
adoption, deployment, and stable-publication evidence remain pending or
blocked with their existing owner/operator boundaries.

## Deviations

- The local Docker daemon is unavailable/non-responsive; the repository keeps
  the container check enabled and reports the failure rather than skipping it.
- Existing historical release and security identities were not rebound.
- The exact repository revision may advance through generated evidence-only
  commits; final launch receipts must be regenerated after the Judge audit and
  final local source revision is established.
