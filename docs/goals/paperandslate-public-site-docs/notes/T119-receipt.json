{
  "goalbuddy_receipt_v1": {
    "result": "done",
    "task_id": "T119",
    "board_path": "docs/goals/paperandslate-public-site-docs/state.yaml",
    "decision": "approved",
    "full_outcome_complete": false,
    "rationale": "T118's failures are generated-runtime corruption, not established source or dependency regressions. Current apps/web/.next is the exact owned target, has no reparse points, and port 3101 is free. Its development log records missing manifests, missing Turbopack runtime modules, and ENOENT errors. Approve one sole sequential artifact-recovery Worker using the existing guarded build boundary, followed by browser verification. Preserve all T118 source, provenance, docs/search artifacts, and snapshots.",
    "worker_package": {
      "objective": "Recover fresh local runtime evidence using only the exact generated apps/web/.next target: run the existing guarded build:verify first to remove and recreate it and prove a non-standalone production build, then run the configured Playwright docs, smoke, and accessibility suite sequentially on free 127.0.0.1:3101. Preserve every T118 source and generated provenance/docs/search file and all three unchanged homepage snapshots. Do not modify source, dependencies, reports, plans, snapshots, external systems, or .turbo.",
      "allowed_files": ["apps/web/.next", "test-results/.last-run.json"],
      "verify": ["$target=(Resolve-Path -LiteralPath 'apps/web/.next').Path; $expected=[IO.Path]::GetFullPath((Join-Path (Get-Location) 'apps/web/.next')); if($target -ne $expected){throw 'Unexpected .next target'}; $root=Get-Item -LiteralPath 'apps/web/.next' -Force; if($root.Attributes -band [IO.FileAttributes]::ReparsePoint){throw '.next root is a reparse point'}; $bad=@(Get-ChildItem -LiteralPath 'apps/web/.next' -Recurse -Force | Where-Object {$_.Attributes -band [IO.FileAttributes]::ReparsePoint}); if($bad){$bad.FullName; exit 1}", "$listener=@(Get-NetTCPConnection -State Listen -LocalPort 3101 -ErrorAction SilentlyContinue); if($listener){$listener | Format-Table; exit 1}", "pnpm build:verify", "$listener=@(Get-NetTCPConnection -State Listen -LocalPort 3101 -ErrorAction SilentlyContinue); if($listener){$listener | Format-Table; exit 1}", "pnpm exec playwright test tests/browser/docs-core.spec.ts tests/browser/smoke.spec.ts tests/browser/a11y.spec.ts", "Before and after the build/browser sequence, SHA256-hash all T118 changed source/generated files and tests/browser/snapshots/homepage.png, tests/browser/snapshots/homepage-mobile.png, and tests/browser/snapshots/homepage-dark.png; fail on any hash change."],
      "stop_if": ["The validated target is not exactly apps/web/.next, is a reparse point, or cleanup would traverse a reparse point.", "Port 127.0.0.1:3101 is occupied before either verification phase; do not stop the occupying process.", "Any file outside apps/web/.next or test-results/.last-run.json changes, including source, dependencies, reports, plans, generated provenance/docs/search, .turbo, or snapshots.", "Any T118 protected source/generated hash or homepage snapshot hash changes.", "The existing guarded build boundary fails with ENOTEMPTY, manifest, Turbopack, reparse-point, or other corruption twice for the same cause.", "Browser or build failure indicates a source, dependency, configuration, or product defect rather than stale generated artifacts.", "Any snapshot update, visual:update invocation, visual-pass claim, parity claim, production-acceptance claim, commit, deployment, or external-system action is proposed."]
    },
    "evidence": ["T118 records two browser failures from .next/dev manifest/Turbopack corruption and two build failures removing .next/dev with ENOTEMPTY.", "apps/web/.next is an ordinary directory with zero reparse points; its dev logs contain missing manifests, missing temporary build files, and missing Turbopack runtime modules.", "scripts/build-boundary.ts validates the exact .next path, rejects reparse roots, cleans only that generated tree, checks source stability, and runs a verification-mode production build.", "Port 3101 is free; unrelated listeners on 3001 and 3002 must remain untouched.", "T113/T117 establish current truthful homepage source authority and unchanged snapshots as baseline-drift evidence."],
    "subgoal_contract": null,
    "parallel_safety": {"safe": false, "reason": "Build and browser verification share apps/web/.next and port 3101; the package must run as one sole sequential Worker with no concurrent Next, Playwright, or Turbo activity."},
    "blocked_tasks": ["Accepted visual QA remains unresolved because unchanged snapshots are baseline-drift evidence.", "External repository, DNS/TLS, Tower/Coolify, provider, credential, deployment, monitoring, legal, factual, Lighthouse, SBOM, and production-acceptance evidence remains pending."],
    "missing_evidence": ["Fresh post-recovery production-boundary build evidence.", "Fresh post-recovery browser and accessibility evidence.", "Post-run proof that all T118 protected files and snapshots remain byte-identical.", "Independent Git diff is unavailable because web is not a Git checkout; preservation must rely on receipt and hash checks."],
    "required_board_updates": ["Record T119 as done with decision approved and full_outcome_complete false.", "Create exactly one sole active Worker using worker_package verbatim.", "Preserve T118 source, generated provenance/docs/search artifacts, current homepage authority, and unchanged snapshots.", "Do not mark the overall goal complete after artifact recovery; require a later milestone audit for remaining local and external evidence."]
  }
}
