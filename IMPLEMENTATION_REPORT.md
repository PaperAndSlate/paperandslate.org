# Implementation report

> Historical implementation receipts. For the current audit result, use [`AUDIT_VERIFICATION_REPORT.md`](AUDIT_VERIFICATION_REPORT.md). Claims in this file are not current production-readiness evidence.

T031 recovered the production verification after an exact-target Node cleanup of `apps/web/.next`; `pnpm build` passed on the first clean attempt. T031's content/feeds/release/routes/search/typecheck and full test checks also pass. No documentation source or unrelated app files were modified.

T007 adds local launch hardening and evidence tooling. Security headers use a static CSP without production `unsafe-eval`; HSTS is emitted only for HTTPS requests. Metadata, canonical URLs, Open Graph, JSON-LD, sitemap, robots, preview noindex behavior, health metadata, newsletter response headers, accessibility semantics, redaction, browser tests, budgets, rollback manifests, CI hooks, and non-root standalone Docker packaging are present.

External providers, Tower/Coolify, Infisical, DNS/TLS, remote repositories, production deployment/monitoring, and legal/factual approval remain pending. Browser, axe, Lighthouse, and Docker results are reported only when locally available.

T038 repaired the development-only CSP incompatibility in `apps/web/src/middleware.ts`: development responses assemble the React Refresh `unsafe-eval` source, while production responses retain the strict CSP without it. The existing SearchDialog markup and focused assertions were unchanged. After exact-target cleanup, the dedicated Playwright server launched the real Paper & Slate app on `http://127.0.0.1:3101` with `reuseExistingServer: false`; `pnpm e2e` passed all 4 smoke tests, including the header-dialog contract, and `pnpm a11y` passed all 4 checks. `pnpm security:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (21 tests), and `pnpm build` also passed.

Historical T044 evidence recorded Playwright, axe, visual-update, and visual-check results; subsequent T112 verification superseded the visual claim: visual verification failed twice for unchanged light, mobile, and dark homepage snapshots, and the snapshots remain unchanged. The homepage baseline is at `tests/browser/snapshots/homepage.png`. The standalone Docker image build passed. The retained diagnostic container initially exited with `MODULE_NOT_FOUND: next`; the bounded Dockerfile symlink repair was rebuilt and retried once. The repaired container stayed up on free loopback port `127.0.0.1:3111`, logged `Next.js 15.2.2` and `Ready`, and `Invoke-WebRequest http://127.0.0.1:3111/health` returned HTTP 200 with status `ok`, local deployment/build metadata, docs source count, lock hash, and static-fallback search mode. The named diagnostic container was then removed; existing services were untouched. Lighthouse remains unavailable because its CLI/browser audit is not installed; no Lighthouse result is claimed.

T044 repeated the exact generated-output cleanup for `apps/web/.next` using the guarded path-equality command. Against the real Paper & Slate app on the configured dedicated server `http://127.0.0.1:3101` with `reuseExistingServer: false`, `pnpm e2e` passed all 4 smoke tests and `pnpm a11y` passed all 4 accessibility checks. `pnpm build` passed with 3 successful tasks, compiling the web app and generating 80 static pages. The build emitted only the observed existing autoprefixer and Next.js ESLint-plugin warnings. No source or search records were changed. Lighthouse, production, and external-provider checks remain pending.

T104–T114 evidence refresh: T104–T111 cover the local documentation/design foundation and verification history. T112 passed install, audit, routes/content/docs, SEO/security, lint, typecheck, tests, browser/accessibility, and build checks, while visual verification failed twice for unchanged light, mobile, and dark homepage snapshots. T113 kept the mismatch as historical evidence; T114 records the PM decision that the current truthful homepage source governs visual acceptance and that legacy snapshots remain unchanged baseline-drift evidence. This report does not convert that decision into a visual pass.

T120 recovered fresh local runtime evidence: the guarded non-standalone build generated 102 static pages, the docs-core/smoke/accessibility Playwright suite passed 7/7, and all 14 protected source/generated and homepage-snapshot hashes remained unchanged. T122 records the central Fumadocs documentation wiring and its local docs/content, lint, typecheck, build, and browser evidence; its receipt remained blocked by the then-existing format script and stale test boundaries, so it is not presented as a complete gate. T130 subsequently records the bounded eight-file formatting refresh, passing format:check, focused docs/content tests and validation, scoped ESLint, and both TypeScript checks. These are local evidence only.

Remaining blockers are Lighthouse measurement, accepted current-source visual QA, qualified legal/privacy/trademark/institutional/maintainer/funding/factual review, media provenance, Typesense/Kit/GlitchTip/Infisical/provider credentials and activation, Tower/Coolify, remote repository/branch/PR, DNS/TLS, deployment, production monitoring, rollback drill, external syndication/publication/feed validation, and SBOM/release identity. No production-readiness, provider-activation, legal/factual-approval, deployment, Git SHA, DNS/TLS, monitoring, or production claim is made.
